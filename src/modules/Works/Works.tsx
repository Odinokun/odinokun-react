import { FC } from 'react';
import { PageTitle } from '@common/components/PageTitle/PageTitle.tsx';

interface IProps {}

export const Works: FC<IProps> = () => {
  return (
    <div>
      <PageTitle title="Works title" />
    </div>
  );
};
